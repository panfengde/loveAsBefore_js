(+ 1 1)
(define a 10)
a

((lambda (x) (+ x 1) (+ x 1000)) 10)


(let 
    (
        (a 10)
    )
    (+ a 100)
    
    )

(define-syntax let*
    (syntax-rules ()
      ((_ ((p v)) b ...)
       (let ((p v)) b ...))
      ((_ ((p1 v1) (p2 v2) ...) b ...)
       (let ((p1 v1))
         (let* ((p2 v2) ...)
          b ...))))
          )

(cons 1 2)


(let* 
    (
        (a 10)
        (b (+ a 10))
    )
    (display b)
)