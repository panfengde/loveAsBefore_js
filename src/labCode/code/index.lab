


; (define a 10)
; (set! a 15)
; a

(display "goo")

(let* 
    (
        (a 10)
        (b (+ a 10))
    )
    (cond
        ((= b 10) (display "b"))
        ((= a 10) (display "a"))
    )
)

(display "let--cond")

(let 
    (
        (n 3)
    )
    (display n)
    (cond 
        ((= n 2) 1)
        ((= n 3) 3)
        (else "other")
    )
    (display "ok")
)

