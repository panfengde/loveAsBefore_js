(define a (json 
    (: "hello" 1)
    (: "hello2" 2)
    (: "hello3" (lambda (x) (display x)))
    )
)

a
(set! (. a "hello") 1)
(set! (. a "hello2") 6)
((. a "hello3") "?????")

(display (. a "hello"))

(set! (.. a "hello" "go"))

(.. a "hello" "go")