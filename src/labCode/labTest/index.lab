(define-syntax let*
    (syntax-rules ()
      ((_ ((p v)) b ...)
       (let ((p v)) b ...))
      ((_ ((p1 v1) (p2 v2) ...1) b ...2)
       (let ((p1 v1))
         (let* ((p2 v2) ...1)
          b ...2))))
)
(define a 10)
a
(+ 1 1)
'(a b)
